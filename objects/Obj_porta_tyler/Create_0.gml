nome = "Porta";

// No evento Create de Obj_porta_tyler

// Isto garante que o diálogo funcione como antes
nome = "Porta";

// E estas duas linhas param a animação no início
image_speed = 0; // Velocidade da animação = 0 (parada)
image_index = 0; // Garante que ela pare no primeiro frame (frame 0)